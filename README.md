- start production mode v.0.1.0 after final test mode v0.0.9
- start working on test branch after v0.0.9

___

###TODO LIST
- start with single db and two tables to ensure stability
- implement json-config.py structure.
- if new db created append the conf to log.json, if updated update the log.json with date.
- build setup to to take everything from config.py and construct for the gvien session.
- build config.py to take from latest logs from json and structrure the system. (memory)
- build while loops to ensure healthy cli journey.

